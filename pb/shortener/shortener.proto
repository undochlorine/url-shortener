syntax = "proto3";
package shortener;
option go_package = "./shortener";

message ShortUrlMsg {
  string short_url = 1;
}

message FullUrlMsg {
  string full_url = 1;
}

service Shortener {
  rpc Get(ShortUrlMsg) returns (FullUrlMsg){}
  rpc Set(FullUrlMsg) returns (ShortUrlMsg){}
}