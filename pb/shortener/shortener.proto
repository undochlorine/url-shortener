syntax = "proto3";
package shortener;
option go_package = "./shortener";

message ShortUrl {
  string short_url = 1;
}

message FullUrl {
  string full_url = 1;
}

service Shortener {
  rpc Get(ShortUrl) returns (FullUrl){}
  rpc Set(FullUrl) returns (ShortUrl){}
}